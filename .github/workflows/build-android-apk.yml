name: Build Android APK

on:
  push:
    branches:
      - agent/build-android-apk
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Install Capacitor dependencies
        run: npm ci
      - name: Build test APK
        working-directory: android
        run: ./gradlew assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: wolf-android-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
      - name: Publish test APK
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          cp android/app/build/outputs/apk/debug/app-debug.apk AI-Werewolf-Android-v1.0.apk
          gh release view android-v1.0 >/dev/null 2>&1 || gh release create android-v1.0 --prerelease --title "Android test v1.0" --notes "Android test build of AI狼人杀. This APK opens the hosted PWA and requires internet access."
          gh release upload android-v1.0 AI-Werewolf-Android-v1.0.apk --clobber
